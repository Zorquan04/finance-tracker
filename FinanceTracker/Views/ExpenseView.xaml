<UserControl x:Class="FinanceTracker.Views.ExpenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:vm="clr-namespace:FinanceTracker.ViewModels">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="120" Margin="5" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="80" Margin="5" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged,StringFormat=F2}" />
            <ComboBox Width="120" Margin="5" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name" />
            <DatePicker Width="140" Margin="5" SelectedDate="{Binding Date, Mode=TwoWay}" DisplayDateEnd="{x:Static sys:DateTime.Now}" />
            <Button Width="100" Height="25" Content="{Binding AddButtonText}" Command="{Binding AddExpenseCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <Button Width="120" Height="25" Content="Show filters" Command="{Binding ToggleFiltersCommand}" HorizontalAlignment="Right" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10" Visibility="{Binding FiltersVisible, Converter={StaticResource BoolToVis}}">
            <ComboBox Width="120" ItemsSource="{Binding FilterCategories}" SelectedItem="{Binding SelectedFilterCategory}" DisplayMemberPath="Name" />
            <DatePicker Width="120" SelectedDate="{Binding StartDate, Mode=TwoWay}" DisplayDateEnd="{x:Static sys:DateTime.Now}" Margin="10,0,0,0" />
            <TextBlock VerticalAlignment="Center" Margin="5,0">—</TextBlock>
            <DatePicker Width="120" SelectedDate="{Binding EndDate, Mode=TwoWay}" DisplayDateEnd="{x:Static sys:DateTime.Now}" Margin="0,0,5,0" />
            <Button Content="Clear" Margin="5,0,0,0" Command="{Binding ClearDateRangeCommand}" />
            <TextBlock Margin="20,0,0,0" VerticalAlignment="Center" FontWeight="Bold" Text="{Binding TotalExpenses, StringFormat=Total: {0:N2} zł}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding ExpensesView}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" SelectedItem="{Binding SelectedExpense}" CanUserSortColumns="False"
                      GridLinesVisibility="None" RowBackground="#1E1E1E" AlternatingRowBackground="#252525" Foreground="White" HeadersVisibility="Column" RowHeight="32" FontSize="14">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="Background" Value="#333333"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=F2}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category.Name}" Width="*"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="0,4"/>
                    </Style>
                </StackPanel.Resources>

                <Button Content="Sort Name" Command="{Binding SortByNameCommand}" />
                <Button Content="Sort Amount" Command="{Binding SortByAmountCommand}" />
                <Button Content="Sort Category" Command="{Binding SortByCategoryCommand}" />
                <Button Content="Sort Date" Command="{Binding SortByDateCommand}" />

                <Button Content="Up" Command="{Binding MoveUpCommand}" />
                <Button Content="Down" Command="{Binding MoveDownCommand}" />

                <Button Content="{Binding EditButtonText}" Command="{Binding EditCommand}" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>