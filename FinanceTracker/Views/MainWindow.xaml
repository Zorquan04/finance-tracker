<Window x:Class="FinanceTracker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:v="clr-namespace:FinanceTracker.Views"
        xmlns:vm="clr-namespace:FinanceTracker.ViewModels"
        Title="Finance Tracker"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ChartsViewModel}">
            <v:ChartsView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:BudgetViewModel}">
            <v:BudgetView/>
        </DataTemplate>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open" Command="{Binding OpenCommand}" />
                <MenuItem Header="_Save" Command="{Binding SaveCommand}" />
                <MenuItem Header="Save _As" Command="{Binding SaveAsCommand}" />
                <Separator/>
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}" />
            </MenuItem>
        </Menu>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" DockPanel.Dock="Bottom" Height="40">
            <Button Content="Expenses" Width="120" Margin="5" Command="{Binding ShowExpensesCommand}"/>
            <Button Content="Charts" Width="120" Margin="5" Command="{Binding ShowChartsCommand}"/>
            <Button Content="Budget" Width="120" Margin="5" Command="{Binding ShowBudgetCommand}"/>
        </StackPanel>

        <ContentControl x:Name="MainContent" />
    </DockPanel>
</Window>