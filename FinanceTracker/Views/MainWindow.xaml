<Window x:Class="FinanceTracker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:v="clr-namespace:FinanceTracker.Views"
        xmlns:vm="clr-namespace:FinanceTracker.ViewModels"
        xmlns:res="clr-namespace:FinanceTracker.Resources"
        Title="Finance Tracker"
        Height="480" Width="800"
        MinHeight="480" MinWidth="800"
        Background="{DynamicResource AppBackground}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ChartViewModel}">
            <v:ChartView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:BudgetViewModel}">
            <v:BudgetView/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{x:Static res:AppResources.Menu_File}">
                <MenuItem Header="{x:Static res:AppResources.Menu_Open}" Command="{Binding OpenCommand}" />
                <MenuItem Header="{x:Static res:AppResources.Menu_Save}" Command="{Binding SaveCommand}" />
                <MenuItem Header="{x:Static res:AppResources.Menu_SaveAs}" Command="{Binding SaveAsCommand}" />
                <Separator/>
                <MenuItem Header="{x:Static res:AppResources.Menu_Exit}" Command="{Binding ExitCommand}" />
            </MenuItem>

            <MenuItem Header="{x:Static res:AppResources.Menu_Settings}">
                <MenuItem Header="{x:Static res:AppResources.Menu_ChangeTheme}" Command="{Binding ChangeThemeCommand}" />
                <MenuItem Header="{x:Static res:AppResources.Menu_ChangeLanguage}" Command="{Binding ChangeLanguageCommand}" />
                <MenuItem Header="{x:Static res:AppResources.Menu_ClearData}" Command="{Binding ClearDataCommand}" />
            </MenuItem>

            <MenuItem Header="{x:Static res:AppResources.Menu_Help}">
                <MenuItem Header="{x:Static res:AppResources.Menu_About}" Command="{Binding ShowAboutCommand}" />
                <MenuItem Header="{x:Static res:AppResources.Menu_UserGuide}" Command="{Binding OpenUserGuideCommand}" />
            </MenuItem>
        </Menu>

        <Grid DockPanel.Dock="Bottom" Height="50">
            <Grid.RowDefinitions>
                <RowDefinition Height="15"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="LimeGreen" FontWeight="Bold" Opacity="0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStatusVisible}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <StackPanel Grid.Row="1"
                Orientation="Horizontal"
                HorizontalAlignment="Center">
                <Button Content="{x:Static res:AppResources.Button_Expenses}" Width="120" Margin="5" Command="{Binding ShowExpensesCommand}"/>
                <Button Content="{x:Static res:AppResources.Button_Charts}" Width="120" Margin="5" Command="{Binding ShowChartsCommand}"/>
                <Button Content="{x:Static res:AppResources.Button_Budget}" Width="120" Margin="5" Command="{Binding ShowBudgetCommand}"/>
            </StackPanel>
        </Grid>

        <ContentControl x:Name="MainContent" />
    </DockPanel>
</Window>