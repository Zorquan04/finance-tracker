<Window x:Class="FinanceTracker.Views.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:FinanceTracker.Resources"
        Title="Confirm"
        Height="160" Width="350"
        Background="{DynamicResource AppBackground}"
        WindowStartupLocation='CenterScreen'
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="14"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="{x:Static res:AppResources.Title_Yes}" Width="80" Margin="0,0,10,0" Click="Yes_Click"/>
            <Button Content="{x:Static res:AppResources.Title_No}" Width="80" Margin="0,0,10,0" Click="No_Click"/>
        </StackPanel>
    </Grid>
</Window>